{
  "name": "the-courtyard-monorepo",
  "version": "1.0.0",
  "description": "Root runner for The Courtyard Premium Pickleball Platform",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "expo-linear-gradient": "^56.0.4"
  }
}
